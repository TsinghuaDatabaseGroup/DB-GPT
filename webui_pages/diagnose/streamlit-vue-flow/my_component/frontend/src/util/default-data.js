const defaultOptions = {
  disLinkable: false, // 可删除连线
  linkable: false,    // 可连线
  draggable: false,   // 可拖动
  zoomable: false,    // 可放大
  moveable: true,    // 可平移
  theme: {
    edge: {
      shapeType: 'AdvancedBezier'
    }
  }
};

export {
  defaultOptions,
};
